{
    // Required extensions are LaTeX and PDF Viewer, both from Mathematic Inc
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Build LaTeX Resume",
            "type": "shell",
            "command": "pdflatex -synctex=1 -interaction=nonstopmode -file-line-error ${workspaceFolder}/resume.tex",
            "problemMatcher": [],
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}